{% extends 'base.html.twig' %}

{% block title %}Visualiser le country {{ country.name }} {% endblock %}

{% block body %}
    <div class="container-xl bg-light p-5 border-top">
        <a href="{{ path('app_back_country_browse') }}" class="btn btn-primary">Retour à la liste</a>

        <h1>{{ country.name }}</h1>
        <a href="{{ path('app_back_country_edit', {id: country.id}) }}" class="btn btn-secondary">Modifier</a>
        <a href="{{ path('app_back_country_delete', {id: country.id}) }}" class="btn btn-danger">Supprimer</a>


        <table class="table">
            <tbody>
                <tr><th>Id</th><td>{{ country.id }}</td></tr>
                <tr><th>Nom</th><td>{{ country.name }}</td></tr>
            <tbody>
        </table>

    </div>

{% endblock %}
